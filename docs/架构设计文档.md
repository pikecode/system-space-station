# 客户资源管理系统 — 架构设计文档

**版本：** v1.0  
**日期：** 2026-07-20  
**状态：** 待 Review

---

## 一、技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | React 18 + TypeScript | 类型安全，组件生态成熟 |
| 前端 UI | Ant Design Pro 5.x | 企业后台标准组件库，内置 RBAC 布局 |
| 前端路由 | React Router v6 | 嵌套路由支持角色布局隔离 |
| 前端状态 | Zustand | 轻量，适合中小型后台 |
| 前端请求 | TanStack Query + Axios | 缓存、loading、错误处理统一管理 |
| 后端框架 | NestJS + TypeScript | 模块化，依赖注入，适合 RBAC 权限体系 |
| 数据库 | PostgreSQL | 事务支持强，分成计算原子性有保障 |
| ORM | Prisma | 类型安全，迁移管理清晰 |
| 认证 | JWT（Access Token） | 无状态，前后端分离友好 |
| 小程序端 | Taro 4.x + React + TypeScript | 与 Web 同语言，共享 packages/shared 类型，支持微信小程序 / H5 双端 |
| 工程管理 | pnpm monorepo | 前端、小程序、后端共享 TypeScript 类型定义 |

---

## 二、项目结构

```
system-space-station/
├── apps/
│   ├── web/                  # React 管理后台
│   │   ├── src/
│   │   │   ├── pages/        # 页面（按角色分目录：admin / dept / my）
│   │   │   ├── components/   # 公共组件
│   │   │   ├── hooks/        # 自定义 Hook
│   │   │   ├── services/     # API 请求层
│   │   │   ├── store/        # Zustand 全局状态
│   │   │   └── utils/
│   │   ├── package.json
│   │   └── vite.config.ts
│   │
│   ├── miniapp/              # Taro 小程序（微信 / H5 双端）
│   │   ├── src/
│   │   │   ├── pages/        # 小程序页面
│   │   │   │   ├── login/
│   │   │   │   ├── customers/    # 我的客户列表 + 详情
│   │   │   │   ├── memberships/  # 提交会员申请
│   │   │   │   ├── approvals/    # 审批列表（负责人）
│   │   │   │   └── commissions/  # 分成明细
│   │   │   ├── components/   # 小程序公共组件
│   │   │   ├── services/     # 复用与 web 相同的 API 端点
│   │   │   ├── store/        # 小程序状态（Zustand）
│   │   │   └── app.config.ts # 小程序全局配置
│   │   ├── package.json
│   │   └── project.config.json   # 微信开发者工具配置
│   │
│   └── api/                  # NestJS 后端
│       ├── src/
│       │   ├── modules/      # 业务模块
│       │   ├── common/       # 装饰器、守卫、拦截器、过滤器
│       │   ├── prisma/       # Prisma service
│       │   └── main.ts
│       ├── prisma/
│       │   ├── schema.prisma
│       │   └── migrations/
│       └── package.json
│
├── packages/
│   └── shared/               # 三端共享 TypeScript 类型
│       └── src/
│           ├── types/        # 实体类型（User, Customer, Membership…）
│           └── enums/        # 枚举（UserRole, MembershipStatus…）
│
├── docs/
├── pnpm-workspace.yaml
└── package.json
```

---

## 三、后端模块设计

```
api/src/modules/
├── auth/               # 登录、JWT 签发与验证、权限守卫
├── departments/        # 部门 CRUD
├── users/              # 用户管理（系统管理员 / 负责人 / 成员）
├── customers/          # 客户信息维护
├── memberships/        # 会员申请、审批流转
├── commissions/        # 分成记录计算、结算周期管理
└── config/             # 系统配置（分成比例、结算天数）
```

每个模块遵循标准 NestJS 结构：`controller → service → repository（Prisma）`

---

## 四、数据模型

### 4.1 用户与组织

```prisma
model Department {
  id          String     @id @default(cuid())
  name        String                        // 部门名称
  description String?                       // 部门描述（可选）
  headId      String?                       // 部门负责人 userId，null 表示暂无负责人
  sort        Int        @default(0)        // 排序权重，数字越小越靠前
  status      DeptStatus @default(ACTIVE)   // ACTIVE=正常 INACTIVE=已停用
  createdAt   DateTime   @default(now())    // 创建时间
  updatedAt   DateTime   @updatedAt         // 最后更新时间

  head        User?      @relation("DeptHead", fields: [headId], references: [id])
  members     User[]     @relation("DeptMembers")
  customers   Customer[]
}

model User {
  id           String     @id @default(cuid())
  name         String                        // 姓名
  phone        String     @unique            // 手机号，同时作为登录账号
  email        String?                       // 邮箱（可选）
  avatar       String?                       // 头像 URL
  jobTitle     String?                       // 职位/岗位名称
  passwordHash String                        // 密码哈希，bcrypt 加密
  role         UserRole                      // ADMIN=系统管理员 HEAD=部门负责人 MEMBER=部门成员
  departmentId String?                       // 所属部门 ID，ADMIN 角色可为 null
  status       UserStatus @default(ACTIVE)   // ACTIVE=在职 INACTIVE=已离职/禁用
  lastLoginAt  DateTime?                     // 最后登录时间
  createdAt    DateTime   @default(now())    // 账号创建时间
  updatedAt    DateTime   @updatedAt         // 最后更新时间

  department   Department? @relation("DeptMembers", fields: [departmentId], references: [id])
  headOf       Department? @relation("DeptHead")
  customers    Customer[]  @relation("AssignedCustomers")
  createdCustomers Customer[] @relation("CreatedCustomers")
}

enum UserRole   { ADMIN HEAD MEMBER }       // 系统管理员 | 部门负责人 | 部门成员
enum UserStatus { ACTIVE INACTIVE }         // 在职 | 离职/禁用
enum DeptStatus { ACTIVE INACTIVE }         // 正常 | 已停用
```

### 4.2 客户

```prisma
model Customer {
  id           String         @id @default(cuid())
  name         String                              // 客户姓名
  phone        String                              // 联系手机号
  gender       Gender?                             // 性别（可选）
  birthday     DateTime?                           // 生日（可选，用于节日关怀）
  idCard       String?                             // 身份证号（可选）
  wechat       String?                             // 微信号（可选）
  address      String?                             // 联系地址（可选）
  source       CustomerSource @default(OTHER)      // 来源渠道
  tags         String?                             // 标签，逗号分隔（如"高净值,转介绍"）
  notes        String?                             // 备注信息
  assignedTo   String                              // 当前维护人 userId（1客户:1维护人）
  createdBy    String                              // 录入人 userId（不随维护人变更而变）
  departmentId String                              // 所属部门 ID（随维护人变更同步更新）
  status       CustomerStatus @default(ACTIVE)     // ACTIVE=正常 INACTIVE=已停用
  createdAt    DateTime       @default(now())      // 录入时间
  updatedAt    DateTime       @updatedAt           // 最后更新时间

  assignedUser  User        @relation("AssignedCustomers", fields: [assignedTo], references: [id])
  creator       User        @relation("CreatedCustomers", fields: [createdBy], references: [id])
  department    Department  @relation(fields: [departmentId], references: [id])
  memberships   Membership[]
}

enum Gender         { MALE FEMALE UNKNOWN }        // 男 | 女 | 未知
enum CustomerSource {
  REFERRAL        // 转介绍
  SELF_DEVELOPED  // 自主开发
  ACTIVITY        // 活动获客
  ONLINE          // 线上渠道
  OTHER           // 其他
}
enum CustomerStatus { ACTIVE INACTIVE }            // 正常 | 已停用
```

### 4.3 会员

```prisma
model Membership {
  id            String           @id @default(cuid())
  memberNo      String           @unique            // 会员编号，系统自动生成（如 M202507001）
  customerId    String                              // 关联客户 ID
  memberType    String?                             // 会员类型/等级（如"普通会员""VIP"，由业务自定义）
  fee           Decimal          @db.Decimal(10, 2) // 会员费金额（元）
  startDate     DateTime                            // 会员有效期开始日
  endDate       DateTime                            // 会员有效期结束日
  paidAt        DateTime?                           // 实际收款确认时间（审批通过时由负责人填写）
  status        MembershipStatus @default(PENDING)  // 见枚举
  submittedBy   String                              // 提交申请的成员 userId
  reviewedBy    String?                             // 审批人 userId
  reviewedAt    DateTime?                           // 审批操作时间
  reviewNote    String?                             // 审批备注（拒绝时必填原因）
  refundReason  String?                             // 退款原因
  refundAmount  Decimal?         @db.Decimal(10, 2) // 实际退款金额（可能小于 fee）
  refundAt      DateTime?                           // 退款确认时间
  createdAt     DateTime         @default(now())    // 申请提交时间
  updatedAt     DateTime         @updatedAt         // 最后更新时间

  customer      Customer           @relation(fields: [customerId], references: [id])
  commissions   CommissionRecord[]
}

enum MembershipStatus {
  PENDING        // 待审核：已提交，等待负责人审批
  APPROVED       // 有效：审批通过，分成已生成
  REJECTED       // 已拒绝：审批不通过，可修改后重新提交
  EXPIRED        // 已到期：有效期结束
  REFUND_PENDING // 退款申请中：客户申请退款，等待确认
  REFUNDED       // 已退款：退款完成，已生成负向冲账记录
}
```

### 4.4 分成

```prisma
model CommissionConfig {
  id             String   @id @default(cuid())
  memberRatio    Decimal  @db.Decimal(5, 2)     // 维护人员分成比例（%），三项之和必须 = 100
  headRatio      Decimal  @db.Decimal(5, 2)     // 部门负责人分成比例（%）
  companyRatio   Decimal  @db.Decimal(5, 2)     // 公司分成比例（%）
  settlementDays Int      @default(15)           // 结算周期天数（1-365，默认 15 天）
  effectiveFrom  DateTime                        // 该配置的生效时间，新会员按此时最新配置计算
  remark         String?                         // 配置备注（如"2026年Q3调整"）
  createdBy      String                          // 操作人 userId
  createdAt      DateTime @default(now())        // 配置创建时间
}

model CommissionRecord {
  id           String           @id @default(cuid())
  membershipId String                              // 关联的会员记录 ID
  receiverId   String                              // 收款方 userId；COMPANY 角色填固定虚拟 ID "COMPANY"
  receiverRole CommissionRole                      // 分成角色：MEMBER=维护人 HEAD=负责人 COMPANY=公司
  amount       Decimal          @db.Decimal(10, 2) // 分成金额（元）
  ratio        Decimal          @db.Decimal(5, 2)  // 分成比例快照（审批时固化，不随配置修改变化）
  status       CommissionStatus @default(PENDING)  // 见枚举
  periodId     String?                             // 归属结算周期 ID，未归入周期时为 null
  remark       String?                             // 备注（如冲账说明）
  createdAt    DateTime         @default(now())    // 分成记录生成时间
  settledAt    DateTime?                           // 财务结算确认时间

  membership   Membership        @relation(fields: [membershipId], references: [id])
  period       SettlementPeriod? @relation(fields: [periodId], references: [id])
}

model SettlementPeriod {
  id             String       @id @default(cuid())
  startDate      DateTime                          // 周期开始日期
  endDate        DateTime                          // 周期结束日期
  totalFee       Decimal      @db.Decimal(12, 2)  // 本周期内会员费总额（汇总快照）
  totalAmount    Decimal      @db.Decimal(12, 2)  // 本周期分成总额（汇总快照）
  status         PeriodStatus @default(OPEN)       // OPEN=进行中 PENDING_PAYMENT=待出账 SETTLED=已结算
  settledBy      String?                           // 财务确认操作人 userId
  settledAt      DateTime?                         // 财务确认结算时间
  remark         String?                           // 备注（如"7月第一期"）
  createdAt      DateTime     @default(now())      // 周期创建时间

  records        CommissionRecord[]
}

enum CommissionRole   {
  MEMBER    // 维护人员
  HEAD      // 部门负责人
  COMPANY   // 公司
}
enum CommissionStatus {
  PENDING          // 待结算：会员有效，等待周期汇总
  PENDING_PAYMENT  // 待出账：已归入结算周期，等待财务确认
  SETTLED          // 已结算：财务已确认
  REVERSED         // 已冲账：因退款产生的负向记录
}
enum PeriodStatus {
  OPEN             // 进行中：当前结算周期
  PENDING_PAYMENT  // 待出账：周期结束，等待财务确认
  SETTLED          // 已结算：财务已完成对账
}
```

---

## 五、权限设计（RBAC）

```
ADMIN  → 所有接口
HEAD   → 本部门客户读写 + 审批接口 + 本部门报表
MEMBER → 本人名下客户读写 + 提交会员申请 + 个人分成查看
```

实现方式：NestJS `@Roles()` 装饰器 + `RolesGuard`，结合 JWT payload 中的 `role` 和 `departmentId` 做数据行级过滤。

---

## 六、核心 API 设计

### 认证
```
POST /auth/login          → { token }
GET  /auth/me             → 当前用户信息
```

### 部门
```
GET    /departments        → 部门列表（ADMIN）
POST   /departments        → 创建部门（ADMIN）
PATCH  /departments/:id    → 编辑部门（ADMIN）
DELETE /departments/:id    → 停用部门（ADMIN）
```

### 用户
```
GET    /users              → 用户列表（ADMIN，可按部门筛选）
POST   /users              → 创建用户（ADMIN）
PATCH  /users/:id          → 编辑用户（ADMIN）
PATCH  /users/:id/status   → 启用/停用（ADMIN）
```

### 客户
```
GET    /customers          → 客户列表（按角色过滤数据范围）
POST   /customers          → 创建客户（MEMBER/HEAD）
GET    /customers/:id      → 客户详情
PATCH  /customers/:id      → 编辑客户
PATCH  /customers/:id/transfer → 转移维护人（HEAD）
```

### 会员
```
POST   /memberships                    → 提交会员申请（MEMBER）
GET    /memberships/pending            → 待审批列表（HEAD）
PATCH  /memberships/:id/approve        → 审批通过（HEAD）
PATCH  /memberships/:id/reject         → 审批拒绝（HEAD）
POST   /memberships/:id/refund         → 申请退款
```

### 分成 & 结算
```
GET  /commissions/my                   → 个人分成明细（MEMBER）
GET  /commissions/department           → 部门分成汇总（HEAD）
GET  /commissions/overview             → 全公司汇总（ADMIN）
POST /commissions/settle/:periodId     → 财务确认结算（ADMIN）
```

### 配置
```
GET   /config              → 当前配置
PATCH /config              → 更新分成比例和结算周期（ADMIN）
```

---

## 七、小程序端（Taro）

### 功能定位

小程序面向**一线使用人员**（成员 + 负责人），提供移动端核心操作。系统管理员的组织管理和全局配置仅在 Web 后台操作。

### 小程序页面结构

```
tabBar
├── 客户          /pages/customers/index     我的客户列表
├── 待办          /pages/approvals/index     待审批会员申请（负责人可见）
└── 我的          /pages/profile/index       个人信息 + 分成明细

页面详情
├── /pages/customers/detail     客户详情 + 会员记录
├── /pages/memberships/create   提交会员申请
├── /pages/memberships/detail   会员申请详情（含审批操作）
└── /pages/commissions/list     分成明细列表
```

### 与 Web 后台的差异

| 功能 | Web 后台 | 小程序 |
|------|---------|--------|
| 部门/用户管理 | ✅ | ✗（管理员专属） |
| 分成比例配置 | ✅ | ✗ |
| 新增客户 | ✅ | ✅ |
| 提交会员申请 | ✅ | ✅ |
| 审批会员申请 | ✅ | ✅（负责人） |
| 个人分成查看 | ✅ | ✅ |
| 全公司分成总览 | ✅ | ✗ |
| 财务结算操作 | ✅ | ✗ |

### 复用策略

- `packages/shared` 类型三端共用（web / miniapp / api）
- 小程序 `services/` 调用与 web 完全相同的 REST API 端点
- JWT 认证逻辑一致，token 存 `wx.setStorageSync`

---

## 八、前端页面结构

```
/login

# 系统管理员
/admin/departments          部门管理（列表 + 新建/编辑/停用）
/admin/users                员工管理（列表 + 新建/编辑/停用/调部门）
/admin/customers            全公司客户（只读视图）
/admin/commissions          全公司分成总览 + 结算操作
/admin/config               分成比例 & 结算周期配置

# 部门负责人
/dept/customers             本部门客户列表 + 维护人转移
/dept/approvals             会员申请审批（待审批列表）
/dept/commissions           本部门分成明细

# 部门成员
/my/customers               我的客户列表 + 新增客户
/my/customers/:id           客户详情 + 提交会员申请
/my/commissions             我的分成明细
```

---

## 八、一期实现范围

本次先实现基础组织架构和客户维护：

- [ ] monorepo 项目初始化（pnpm + NestJS + React）
- [ ] Prisma schema + 数据库迁移
- [ ] 登录 & JWT 认证
- [ ] 部门管理 CRUD（后端 + 前端）
- [ ] 用户管理 CRUD（后端 + 前端）
- [ ] 客户信息 CRUD（后端 + 前端）
- [ ] 权限守卫与数据行级隔离

会员管理、分成计算、结算周期作为二期实现。

---

## 九、待确认问题

- [ ] 客户信息字段：除姓名、手机外，是否需要身份证、来源渠道？
- [ ] 负责人本人维护客户时，负责人层比例如何处理？
- [ ] 退款审批由谁发起和确认？
- [ ] 是否需要操作日志（谁改了什么字段、什么时间）？
- [ ] 通知方式：站内消息还是短信/邮件？

---
